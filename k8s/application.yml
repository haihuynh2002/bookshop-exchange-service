bookshop:
  file-service-uri: http://file-service
spring:
  flyway:
    url: jdbc:postgresql://bookshop-postgres/bookshopdb_exchange
  r2dbc:
    url: r2dbc:postgresql://bookshop-postgres/bookshopdb_exchange
  rabbitmq:
    host: bookshop-rabbitmq
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://bookshop-keycloak/realms/PolarBookshop